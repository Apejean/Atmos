# Atmos Mixer Pro - Comprehensive Agent Handover Document

이 문서는 Mac 환경에서 작업하던 기존 Antigravity 에이전트(작성자)가 Windows 환경에서 새로 작업을 이어받을 **새로운 Antigravity 에이전트**에게 프로젝트의 딥-다이브(Deep-dive) 컨텍스트, 기술적 아키텍처, 과거의 치명적 결함들과 그 해결책을 "단 하나의 축약 없이" 상세하게 인계하기 위한 핵심 지침서입니다.

**⚠️ TO NEW AGENT:** 
당신은 이 문서를 통해 이전 에이전트가 무슨 고민을 했고, 왜 특정 아키텍처를 선택했는지 완벽하게 이해해야 합니다. 사용자의 요구사항을 처리할 때 이 문서에 명시된 "금기 사항(Anti-patterns)"을 절대 범하지 마십시오.

---

## 1. 시스템 개요 및 주요 스택
* **프로젝트 명:** Atmos Mixer Pro
* **목적:** 방 탈출 카페 및 테마파크를 위한 다중 룸(Multi-Room) 오디오 믹싱, 라우팅 및 외부 OSC(Open Sound Control) 통합 제어 시스템.
* **코어 기술 스택:**
  * **Frontend:** Flutter (Dart) - Riverpod를 통한 상태 관리
  * **Backend:** Rust (`cpal` 기반 오디오 엔진, `rosc` 기반 OSC 통신)
  * **FFI (Foreign Function Interface):** `flutter_rust_bridge` (v2)를 사용하여 Dart와 Rust 간의 비동기 통신 및 상태 스트림 전달.

---

## 2. 코어 아키텍처 및 상태 동기화 원칙 (CRITICAL)

가장 중요한 시스템 설계 원칙은 **"단일 진실 공급원(Single Source of Truth)은 오직 Rust 백엔드이며, 오디오 렌더링 스레드는 절대 차단(Block)되어서는 안 된다"**는 것입니다.

### 2.1. 상태 관리 (`rust/src/core/state.rs`)
* `GLOBAL_STATE` 객체가 싱글톤 모듈(`lazy_static` 또는 이와 유사한 글로벌 패턴)로 관리됩니다.
* 프론트엔드(Flutter)는 직접 상태를 낙관적으로(Optimistically) 업데이트하지 않습니다. 프론트엔드의 모든 조작은 API 호출을 통해 Rust로 전달되며, Rust가 상태를 변경한 후 `StreamSink`를 통해 프론트엔드로 브로드캐스팅합니다.
* **통신 아키텍처:** 과거에는 `crossbeam`의 MPMC 채널을 통해 상태를 전파하려 했으나, 큐 포화 및 병목 현상으로 "버튼 씹힘" 현상이 발생했습니다. 현재는 `flutter_rust_bridge`의 `StreamSink`를 `GLOBAL_STATE` 내의 `RwLock<Option<StreamSink<GlobalStateDTO>>>` 형태로 직접 들고 있으며, 상태 변경 시 락을 짧게 쥐고 즉시 데이터를 쏘는 방식을 취합니다.

### 2.2. 오디오 믹서 엔진 (`rust/src/audio/mixer.rs`)
* `cpal` 라이브러리를 사용하여 하드웨어 오디오 콜백을 처리합니다.
* **[절대 금기] 오디오 렌더링 콜백 루프(Closure) 내부에서는 절대 무거운 `Mutex` 락을 오랫동안 점유하거나, I/O 작업을 수행하지 마십시오.** 
* 과거 이 콜백 안에서 메모리 해제 로직을 수행하다 락 경합(Lock Contention)이 발생하여 전체 시스템이 데드락에 빠졌던 뼈아픈 역사가 있습니다. 삭제나 무거운 연산은 백그라운드 스레드나 가비지 컬렉터(GC)로 위임하는 패턴을 유지해야 합니다.

---

## 3. 과거 치명적 버그 및 영구 해결 내역 (Do NOT Regress)

다음은 이전 에이전트가 3-Pass Deep Debugging 프로토콜을 통해 박멸한 치명적 버그들입니다. 새로운 기능을 추가할 때 아래의 문제들이 재발하지 않도록 각별히 주의하십시오.

### 3.1. 버튼 무시(씹힘) 및 데드락 현상
* **증상:** 사용자가 테마 시작, 재생, 룸 클리어 버튼을 빠르게 연속 클릭할 때 명령이 무시됨. 3번 중 1번만 동작하는 불안정성.
* **해결책:** 
  1. 중간 상태 큐(Queue)를 제거하고 `StreamSink` 다이렉트 브로드캐스팅으로 통신 지연 제거.
  2. OSC 수신부(`listener.rs`)에서 `sound_cache`를 조회할 때, 전체 락을 쥐고 있는 상태에서 비동기 연산을 수행하지 않도록 락의 스코프를 최소화(`cloned()`로 데이터만 빼내고 즉시 락 해제).

### 3.2. 룸 클리어 더블 클릭 시 BGM 중복 재생 (Idempotency)
* **증상:** "다음 방 브금 재생"이 포함된 룸 클리어 트리거를 더블 클릭하거나 연타할 경우, 백엔드에 두 개의 트랙 인스턴스가 겹쳐서 재생됨.
* **해결책:** `api_clear_room` 등의 API에서 **멱등성(Idempotency)**을 보장하도록 구현. 해당 방이 이미 활성화되었는지, 다음 BGM 루프가 `playing_track_ids`에 이미 등록되어 있는지 2중 체크 후 통과될 때만 스트림을 생성하도록 방어 로직(`Guard`) 추가 완료.

### 3.3. UI 렌더링 글리치 및 디스크 I/O 폭주 (`RoomCard`)
* **증상:** 룸 이름을 텍스트 필드에서 변경할 때마다 (한 글자 칠 때마다) JSON 파일 저장 및 UI 새로고침이 발생.
* **해결책:** `lib/features/dashboard/widgets/room_card.dart`에서 `onChanged`를 제거하고, `FocusNode`를 도입. 사용자가 입력을 마치고 엔터를 치거나 포커스가 벗어날 때(`onSubmitted`)만 상태를 저장하도록 최적화 완료.

---

## 4. 통합 QA 및 엣지 케이스 안전망 (Safety Nets)

극한의 예외 상황에서도 앱이 `Panic`으로 강제 종료되지 않도록 구축된 안전망들입니다.

1. **오디오 아웃풋 채널 바운더리 체크 (`mixer.rs`):**
   * 연결된 오디오 인터페이스(예: Windows WASAPI 디바이스)의 실제 최대 채널 수보다 높은 숫자의 출력 채널이 설정되어 있어도 절대 `Index out of bounds` 패닉이 나지 않도록 코딩되어 있습니다. (`instance.output_channel < out_channels` 체크 필수). 모노/스테레오 다운믹싱 시에도 안전망이 구현됨.
2. **설정 파일 무결성 및 백업 (`config.rs`):**
   * 앱 실행 중 강제 종료 등의 이유로 `config.json`이 손상된(Corrupted) 경우, 파일 파싱에 실패합니다. 
   * 이때 앱이 설정 파일을 빈 깡통으로 덮어써서 데이터를 영구 증발시키지 않도록, `serde_json` 파싱 에러 발생 시 기존 파일을 `.corrupted.json`으로 백업본을 만든 뒤 안전하게 에러를 리턴하거나 기본값으로 구동합니다.
3. **오디오 파일 누락 관용성 (Fault Tolerance):**
   * 트랙에 매핑된 `*.wav` 파일 중 일부가 디스크에서 삭제되었더라도, 전체 프리로딩(`api_preload_all_sounds`) 과정이 패닉하지 않습니다. 누락된 파일에 대해서만 에러 로그를 남기고, 나머지 정상 파일들은 끝까지 캐싱하도록 수정되었습니다.
4. **OSC 소켓 포트 충돌 방어 (`listener.rs`):**
   * OSC 리스너가 바인딩에 실패(포트 사용 중)하더라도 `.unwrap()`으로 자폭하지 않고 우아하게 에러를 시스템 로그 뷰어로 출력합니다.

---

## 5. Windows 환경 개발 시 특별 주의사항

1. **OS 특정 오디오 드라이버 차이:**
   * Mac에서는 CoreAudio가 사용되었으나, Windows 환경의 `cpal`은 기본적으로 **WASAPI**를 사용합니다. 
   * 디바이스 조회 기능(`apiGetOutputDevices`) 호출 시 디바이스 이름이 다르게 파싱될 수 있습니다. 사용자로부터 "채널 탐색 실패" 이슈가 윈도우에서 재발한다면 디바이스 문자열 매칭 및 채널 개수 조회(`supported_output_configs()`) 로직을 최우선으로 디버깅하십시오.
2. **권한 및 경로 차이:**
   * Windows 환경은 파일 경로 구분자가 `\`이며, 드라이브 문자(`C:\`)를 포함합니다. Rust 백엔드 내에서 경로 조합 시 반드시 `std::path::PathBuf`를 사용하여 크로스 플랫폼 호환성을 유지해야 합니다.

## 6. 새로운 에이전트를 위한 Action Item
1. 사용자가 Windows 터미널 환경에 진입하여 이슈를 보고하면, 가장 먼저 현재 Git 브랜치 상태와 최신 코드가 로컬에 반영되어 있는지 확인하십시오.
2. 이 문서의 원칙에 반하는 `unwrap()` 남용, 렌더링 스레드 내부의 Blocking 연산 도입, 프론트엔드의 성급한 상태 업데이트 로직이 발견되면 즉시 경고하고 수정하십시오.
3. 당신의 임무는 이 견고한 기반 위에서 기능을 확장하고, Windows 특정 버그를 해결하는 것입니다. Good luck.
